PORT=5000
MONGODB_URI=mongodb://127.0.0.1:27017/4tyrezz
JWT_SECRET=change-this-to-a-long-random-string
JWT_EXPIRES_IN=7d
CLIENT_URL=http://localhost:5173
ADMIN_URL=http://localhost:5174
# Mock OTP mode: always issues 123456 and returns it in the API response
# so you can log in without a real SMS provider. Set to false + wire up
# Twilio/Firebase before going to production.
MOCK_OTP=true
